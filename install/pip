name="pip"
arg="install"
install="$name $arg"

packages=(
    classifier
    youtube-dl
    tldr
    Pygments
)

if test ! $(which $name)
then
    echo "Installing a stable version of $name..."
    echo #TODO: PIP installer
fi

$install "${packages[@]}"